import React from 'react'

//This function will connect to the node server using websockets.
// I'm not sure exactly how this works but if it's anything like C than 
// it will return a file descriptior to listen on
export default function connect_to_server() {
	console.log("What is happening?")
}
